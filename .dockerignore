# Build output and dependencies — regenerated inside the image.
node_modules
dist
coverage
.angular

# Local dev / editor files.
.vscode
.idea
*.log
.DS_Store
Thumbs.db

# Git metadata.
.git
.gitignore
.gitattributes

# CI / docs that aren't needed at build time.
.github
docs
*.md
!README.md

# Tests artifacts.
karma.conf.js.bak

# Local env files (the Dockerfile copies environment.example.ts as fallback).
.env
.env.local

# Toolchain folders shipped at the repo root.
../.tools
.tools
